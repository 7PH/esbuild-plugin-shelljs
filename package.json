{
  "name": "esbuild-plugin-shelljs",
  "version": "1.0.0",
  "description": "Patch at build-time the shelljs package to make it compatible with esbuild",
  "main": "build/src/index.js",
  "scripts": {
    "clean": "rm -rf build",
    "build": "npm run clean && tsc",
    "test": "npm run build && node build/test/build.js && node build/test/out.js"
  },
  "files": [
    "build/src/*",
    "patches/*"
  ],
  "author": "7PH <b.raymond@protonmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.1",
    "@types/shelljs": "^0.8.15",
    "@typescript-eslint/eslint-plugin": "^7.12.0",
    "@typescript-eslint/parser": "^7.12.0",
    "esbuild": "^0.21.4",
    "eslint": "^8.57.0",
    "patch-package": "^8.0.0",
    "shelljs": "^0.8.5",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "shelljs": "^0.8.5",
    "eslint": "*"
  }
}
